import type { InnerStoreOptions } from "./types";

// A special identifier within the store generated by createStore
export const __RESY_BRAND__ = Symbol("resy_brand_symbol");

// prefix for computed
export const __COMPUTED_PREFIX__ = "$";

export const DEFAULT_OPTIONS: InnerStoreOptions = {
  unmountRestore: true,
  namespace: undefined,
  immutable: undefined,
  enableMarcoActionStateful: undefined,
  __enableMacros__: undefined,
  __functionName__: "createStore",
};
